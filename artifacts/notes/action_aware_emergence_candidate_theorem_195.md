# Action-aware emergence candidate theorem 195

## Result

The preserved G5 history source contains the native branch-and-arrival core

    G3 -> G4_plus  -> G5
    G3 -> G4_minus -> G5

The two histories meet at the same vertex G5 but arrive through the distinct
mirror-exchanged edges plus_out and minus_out. The vertex records where the
action arrived. The incoming edge records how it arrived.

The old source declares the plus history to be source ordered and the minus
history to be its derived mirror. This anchors the unique sign character

    chi(plus_out)  = +1
    chi(minus_out) = -1

among the two mirror-equivariant sign characters.

## New construction

The post-G5 continuation is newly constructed:

    (plus_out,G5)  -> 1_after_6_plus  -> plus_7  -> 8_plus
    (minus_out,G5) -> 1_after_6_minus -> minus_7 -> 8_minus

The forward branch selects H and conditional offset 1. Reversing the arriving
action exchanges the complete branch with the H_inverse branch and conditional
offset 2.

Both signed continuations project to the same bare drawing tail:

    6 -> 1 -> 7 -> 8

The quotient forgets orientation. The incidence-aware receipt preserves it.

## Provenance boundary

The native G5 branch core, incoming edges, mirror exchange, and source-order
declaration belong to the old preserved source.

The signed return state and the continuations through stages 7 and 8 are a new
author-supplied construction. They are not represented as recovered old-source
transitions.

This theorem is relational. It does not select offset 1 without a directed
arriving action. It does not contradict Artifact184, which proves that the
closed native structure supplies no absolute orientation sign.

No complete native G900 transport operator or physics theorem is claimed.

## Keeper

The old source supplies the arriving sign carrier. Lawful action carries that
sign through the receipt.
