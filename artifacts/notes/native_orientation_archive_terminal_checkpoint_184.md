# Native orientation archive terminal checkpoint 184

## Locked result

The native mirror theorem remains unchanged:

- reflected offset 4 is intrinsic;
- offsets 1 and 2 remain exchanged;
- no premultiplier-clean selector for offset 1 has been derived.

## Receipt Gram

For r_plus = (1,2,1,3,1) and r_minus = (1,3,1,2,1), the Gram
matrix is [[16,15],[15,16]], with eigenvalues 31 and 1.

The antisymmetric eigenline is canonical. Its sign is not. Receipt exchange
reverses the orientation mode while preserving the Gram matrix. The overlap
15 is not a derivation of fifteen independent binary registrations and does
not establish a B32K cardinality theorem.

## G5 lift

G5 contains a split C2 x C2 extension and a nonsplit C4 extension. The
literal oriented G5 action has order 2 and square sign +1, selecting the split
class. The nonsplit C4 class exists but is not forced.

Its two order-four lifts have transition pairs (+1,-1) and (-1,+1). They are
H and H inverse. Both have square sign -1. Their square detects the lifted
class but cannot select a generator direction.

## Archive boundary

The bounded tracked-source audits found no Audit019-free first-step law mapping
one C4 lift to the positive rather than negative reflected D5 generator.
Artifact 180 therefore remains governing authority:

    offset 4 is native;
    offsets 1 and 2 remain exchanged.

This is not a global impossibility theorem. New independently sourced
orientation structure may reopen the gate.

## Keeper

The archive contains the mirror and both arrows. It contains no rule choosing
one arrow.
